document.addEventListener("keydown", (event) => {
    const key = event.key
    if (key === "=") {
        equals()
    }
})
document.addEventListener("keydown", (event) => {
    const key = event.key
    if (key === "=") {
        equals()
    }
})
document.addEventListener("keydown", (event) => {
    const key = event.key
    if (key === "=") {
        equals()
    }
})